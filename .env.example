# Copy this file to .env and fill in your values
DB_USER=root
DB_PASSWORD=your_db_password_here
DB_NAME=dashboard
DB_HOST=127.0.0.1
DB_PORT=3306

# For Cloud Run (Cloud SQL socket path — set this in Cloud Run env vars)
# INSTANCE_CONNECTION_NAME=your-project:region:instance-name

USE_MOCK_DB=false
PORT=8080
